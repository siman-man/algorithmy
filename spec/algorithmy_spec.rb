require 'spec_helper'

describe Algorithmy do
  it 'has a version number' do
    expect(Algorithmy::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
